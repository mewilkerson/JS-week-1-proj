// Problem # 1
// Need to 1) locate price for every item -- use filter method?, 2) add all the prices together and divide by total number of prices, 3) console.log the answer
var average = items.filter (function(itemPrice){
  return items.price;
}

  );


// Problem #2
// 



// Problem # 4
// 

var material = ["plastic", "metal", "glass", "cloth"];

  var hasWood = function(materials) {

      var containsWood = false;

material.forEach(function(currentMaterial){ 
  if(currentMaterial === "wood"; {}
    containsWood = true;
      }
    });
    return containsWood;
  }

  var itemsWithWood = items.filter(function(currentItem){
  var currentMaterials = currentItem.materials;
  var containsWood = hasWood(currentMaterials);
  return containsWood;

});